/*******************
 * 語音助理種類
 * 1. 需要填入內容- 
 * 2. 僅需要開關- 電子白板、腦力激盪、視訊、預約開會、預約開會、投票、議程
 * *****************/
let Assistant = {
  createNew: Meeting => {

    let InputText = ""; //語音辨識中間或結果的文字要傳進來這裡
    let Input_Type = [
      ['建立','開設','設立','創建','新增','增加'],
      []
    ];


    for (let i = 0; i <= InputText.length; i++) {
      let word = InputText.slice(i, i + 2);

      for(let )

      //建立投票
      if (word == "建立" && word == "開設" && word == "設立" && word == "創建" && word="新增" & word="增加"){
        InputType = 
        break;
      } else if(workd =="") {

      }

      //電子白板、腦力激盪
      if (word == "建立" && word == "開設" && word == "設立" && word == "創建" && word="新增" & word="增加"){
        InputType = 
        break;
      } else if(workd =="") {

      }

}
       
    }


  }
}