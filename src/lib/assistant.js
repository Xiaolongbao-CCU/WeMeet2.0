/*******************
 * 語音助理種類
 * 1. 需要填入內容- 預約開會、投票、議程
 * 2. 僅需要開關- 電子白板、腦力激盪、視訊
 * *****************/
let Assistant = {
  createNew: Meeting => {

    let InputText = "";
    let Input_Type = [];


    for (let i = 0; i <= InputText.length; i++) {
      let word = InputText.slice(i, i + 2);
      if (word == "建立" && word == "開設" && word == "設立" && word == "創建" && word="新增" & word="增加"){
        InputType = 
        break;
      } else if(workd =="") {

      }

}
       
    }


  }
}